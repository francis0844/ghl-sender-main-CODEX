@AGENTS.md

## Git workflow

- Always commit and push directly to `main` after every change.
- Never create feature branches. There is only one branch: `main`.
- Never open pull requests. Changes go straight to `main`.
- After every commit, immediately run `git push -u origin main`.

